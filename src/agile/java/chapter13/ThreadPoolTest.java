package agile.java.chapter13;

import junit.framework.TestCase;

public class ThreadPoolTest extends TestCase {

	//case1:
	//生成100个任务(每个任务等待固定时间)，1个工人执行
	
	//生成100个任务(每个任务等待固定时间)，多个工人执行
	
	//生成N个任务(每个任务等待固定时间)，M个工人执行
	
	//case2:
	//一边生成任务，一边由工人执行
}
